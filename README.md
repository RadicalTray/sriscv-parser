# SRISCV Parser
A hastily put together and likely very buggy LL(1) parser for SRISCV made in Zig

- To build and run, grab Zig 0.15.2 at https://ziglang.org/download, and run

  ```
  /path/to/zig build run -- /path/to/sriscv_file
  ```
